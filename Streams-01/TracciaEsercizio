

Inside the src folder define a file called file.txt that has the following lines:

hello
how
are you
?

1) create a tester class that uses the Stream API to try to read each line of file.txt

2) catch a potential IOException